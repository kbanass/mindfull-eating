# Copy to .dev.vars (that file is in .gitignore, never commit it).
# Only needed for `drizzle-kit generate`/`push` over the D1 HTTP API —
# `wrangler dev` and `wrangler d1 migrations apply` don't require them.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
